<div class="form-category">
  <%= form_for [:admin, @category], remote: true do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
    <%= f.label :description %>
    <%= f.text_area :description, row: "3", class: "form-control" %>
    <div class="modal-footer">
      <button type="button" class="btn btn-default" data-dismiss="modal"><%= t(:lb_close) %></button>
      <%= f.submit t(:lb_save), class: "btn btn-primary" %>
    </div>
  <% end %>
</div>
