<% provide :title, "Home" %>
<% if logged_in? %>
  <div class="row">
    <div class="col-md-8">
      <div class="activity">
        <h3><%= t("labels.activities") %></h3>
        <%= render "users/user_activity" %>
      </div>
    </div>
  </div>
<% else %>
  <div class="center jumbotron">
    <h2><%= t("labels.welcome_framgia_e_leaning") %></h2>
    <h4><%= t("labels.signup_now") %></h4>
    <%= link_to t("links.sign_up_now"), signup_path, class: "btn btn-lg btn-primary" %>
  </div>
<% end %>
