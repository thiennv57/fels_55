<li>
  <%= gravatar_for user,size: 50 %>
  <%= link_to user.username, user %>
  <% if logged_in? && current_user.admin? && !current_user?(user) %>
  | <%= link_to t(:lb_delete), user, method: :delete, data: {confirm: "You sure"} %>
  <% end %>
</li>
